<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Project List View -->
    <record id="view_jira_project_list" model="ir.ui.view">
        <field name="name">jira.project.list</field>
        <field name="model">jira.project</field>
        <field name="arch" type="xml">
            <list string="Projects" decoration-muted="state=='cancelled'"
                  decoration-success="state=='completed'">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="key"/>
                <field name="project_lead_id" widget="many2one_avatar_user"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="state" widget="badge"
                       decoration-info="state=='draft'"
                       decoration-success="state=='active'"
                       decoration-warning="state=='on_hold'"
                       decoration-muted="state=='cancelled'"/>
                <field name="sprint_count"/>
                <field name="active_sprint_count"/>
                <field name="ticket_count"/>
            </list>
        </field>
    </record>
     <record model="ir.actions.act_window" id="action_jira_sprint_new">
         <field name="res_model">jira.sprint</field>
        <field name="view_mode">form</field>
        <field name="domain">[('project_id','=',active_id)]</field>
        <field name="context">{'default_project_id': active_id}</field>
    </record>
    <!-- Action Sprint par Projet -->
<record id="action_jira_sprint_from_project" model="ir.actions.act_window">
    <field name="name">Project Sprints</field>
    <field name="res_model">jira.sprint</field>
    <field name="view_mode">kanban,list,form</field>
    <field name="domain">[('project_id', '=', active_id)]</field>
    <field name="context">{
        'default_project_id': active_id,
        'kanban_view_ref': 'jira_project.view_jira_sprint_kanban'
    }</field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            No sprints in this project yet
        </p>
    </field>
</record>
    <!-- Project Kanban View -->
    <record id="view_jira_project_kanban" model="ir.ui.view">
        <field name="name">jira.project.kanban</field>
        <field name="model">jira.project</field>
        <field name="arch" type="xml">
            <kanban
                class="o_hr_recruitment_kanban"

                sample="1"
                limit="40"
                action="%(action_jira_sprint_from_project)d" type="action">
                <field name="id"/>
                <field name="name"/>
                <field name="key"/>
                <field name="color"/>
                <field name="state"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="sprint_count"/>
                <field name="ticket_count"/>
                <field name="project_lead_id"/>

                <templates>
                <t t-name="kanban-box">
    <t t-set="state_color" t-value="record.state.raw_value == 'active' ? '#28a745' : record.state.raw_value == 'completed' ? '#17a2b8' : record.state.raw_value == 'on_hold' ? '#ffc107' : record.state.raw_value == 'cancelled' ? '#dc3545' : '#6c757d'"/>

    <div style="border-radius: 12px; overflow: hidden; border: 1px solid #dee2e6; background: white;">

        <!-- HEADER bleu -->
        <div style="background-color: #185FA5; padding: 16px;">
            <div style="display: flex; justify-content: space-between; align-items: start;">
                <div>
                    <strong style="font-size: 16px; color: white; font-weight: 600;">
                        <field name="name"/>
                    </strong>
                    <div style="color: #B5D4F4; font-size: 12px; margin-top: 2px;">
                        <field name="key"/>
                    </div>
                </div>
                <div class="o_dropdown_kanban dropdown">
                    <a class="dropdown-toggle o-no-caret btn" role="button"
                       data-bs-toggle="dropdown" href="#" aria-label="Dropdown menu">
                        <span class="fa fa-ellipsis-v" style="color: white;"/>
                    </a>
                    <div class="dropdown-menu" role="menu">
                        <a role="menuitem" type="edit" class="dropdown-item">Edit</a>
                        <a role="menuitem" type="delete" class="dropdown-item">Delete</a>
                        <ul class="oe_kanban_colorpicker" data-field="color"/>
                    </div>
                </div>
            </div>
        </div>

        <!-- CONTENU blanc -->
        <div style="padding: 16px;">
            <!-- Dates -->
            <div style="display: flex; gap: 16px; margin-bottom: 12px;">
                <div>
                    <div style="font-size: 11px; color: #6c757d;">Start</div>
                    <div style="font-size: 13px; font-weight: 500;"><field name="start_date"/></div>
                </div>
                <div>
                    <div style="font-size: 11px; color: #6c757d;">End</div>
                    <div style="font-size: 13px; font-weight: 500;"><field name="end_date"/></div>
                </div>
            </div>

            <!-- Stats -->
            <div style="display: flex; gap: 12px; margin-bottom: 12px;">
                <span style="background: #e8f0fe; color: #185FA5; padding: 4px 10px; border-radius: 12px; font-size: 12px;">
                    <i class="fa fa-tasks"/> <field name="sprint_count"/> Sprints
                </span>
                <span style="background: #e8f0fe; color: #185FA5; padding: 4px 10px; border-radius: 12px; font-size: 12px;">
                    <i class="fa fa-ticket"/> <field name="ticket_count"/> Tickets
                </span>
            </div>

            <!-- Footer -->
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <field name="project_lead_id" widget="many2one_avatar_user"/>
                <field name="state" widget="label_selection"
                       options="{'classes': {'draft': 'secondary', 'active': 'success', 'on_hold': 'warning', 'completed': 'info', 'cancelled': 'danger'}}"/>
            </div>
        </div>
    </div>
</t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Project Form View -->
    <record id="view_jira_project_form" model="ir.ui.view">
        <field name="name">jira.project.form</field>
        <field name="model">jira.project</field>
        <field name="arch" type="xml">
            <form string="Project">
                <header>
                    <button name="action_set_active" string="Set Active"
                            type="object" class="oe_highlight"
                            invisible="state == 'active'"/>
                    <button name="action_set_completed" string="Complete"
                            type="object" class="oe_highlight"
                            invisible="state == 'completed'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,active,completed"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                         <button class="oe_stat_button"
            type="action"
            name="%(action_jira_dashboard)d"
            icon="fa-dashboard">
        <div class="o_field_widget o_stat_info">
            <span class="o_stat_text">Dashboard</span>
        </div>
    </button>
                        <button class="oe_stat_button" type="object"
                                name="action_view_sprints" icon="fa-tasks">
                            <field string="Sprints" name="sprint_count" widget="statinfo"/>
                        </button>
                        <button class="oe_stat_button" type="object"
                                name="action_view_tickets" icon="fa-ticket">
                            <field string="Tickets" name="ticket_count" widget="statinfo"/>
                        </button>
                    </div>
                    <field name="avatar_128" widget="image" class="oe_avatar"
                           options="{'size': [90, 90]}"/>
                    <div class="oe_title">
                        <label for="name" string="Project Name"/>
                        <h1>
                            <field name="name" placeholder="e.g., Website Redesign"/>
                        </h1>
                        <h2>
                            <field name="key" placeholder="e.g., WEB" class="oe_inline"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="project_lead_id" widget="many2one_avatar_user"/>
                            <field name="manager_id" options="{'no_create': True}"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="color" widget="color_picker"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Project description and goals..."/>
                        </page>
                        <page string="Team" name="team">
                            <field name="team_ids" widget="many2many_tags"
                                   options="{'color_field': 'color'}"/>
                        </page>
                        <page string="Workflow States" name="workflow">
                            <field name="workflow_state_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="name"/>
                                    <field name="category"/>
                                    <field name="is_default"/>
                                    <field name="is_closed"/>
                                    <field name="fold"/>
                                    <field name="color" widget="color_picker"/>
                                </list>
                            </field>
                        </page>
                        <page string="Sprints" name="sprints">
                            <field name="sprint_ids">
                                <list decoration-bf="state=='active'">
                                    <field name="name"/>
                                    <field name="start_date"/>
                                    <field name="end_date"/>
                                    <field name="ticket_count"/>
                                    <field name="progress" widget="progressbar"/>
                                    <field name="state" widget="badge"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Project Search View -->
    <record id="view_jira_project_search" model="ir.ui.view">
        <field name="name">jira.project.search</field>
        <field name="model">jira.project</field>
        <field name="arch" type="xml">
            <search string="Projects">
                <field name="name"/>
                <field name="key"/>
                <field name="project_lead_id"/>
                <filter string="My Projects" name="my_projects"
                        domain="[('project_lead_id', '=', uid)]"/>
                <separator/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Active" name="active" domain="[('state', '=', 'active')]"/>
                <filter string="Completed" name="completed" domain="[('state', '=', 'completed')]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="status" context="{'group_by': 'state'}"/>
                    <filter string="Project Lead" name="lead" context="{'group_by': 'project_lead_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Project Action -->
    <record id="action_jira_project" model="ir.actions.act_window">
        <field name="name">Projects</field>
        <field name="res_model">jira.project</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first project
            </p>
            <p>
                Projects help you organize your work into manageable pieces.
            </p>
        </field>
    </record>
</odoo>